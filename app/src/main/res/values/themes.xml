<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AntfyV2" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/antfy_primary</item>
        <item name="colorPrimaryVariant">@color/antfy_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/antfy_secondary</item>
        <item name="colorSecondaryVariant">@color/antfy_ligth</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- ActionBar Title Theme -->
    <style name="antfyABTitle" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/antfy_primary</item>                     // barra título
        <item name="actionBarTheme">@color/antfy_background</item>                // estilo barra titulo
        <item name="colorPrimaryDark">@color/antfy_dark</item>                    // barra notificação
        <item name="colorAccent">@color/antfy_ligth</item>                        // cor hiperlink
        <item name="android:windowBackground">@color/antfy_background</item>      // fundo
        <item name="android:textColorSecondary">@android:color/white</item>       // icone voltar
        <item name="android:textColorPrimary">@android:color/white</item>         // título
    </style>

    <!-- NoActionBar Theme -->
    <style name="antfyNoAB" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>
</resources>